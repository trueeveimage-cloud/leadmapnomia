databases:
  - name: ruleradar-postgres
    plan: basic-1gb

envVarGroups:
  - name: ruleradar-shared
    envVars:
      - key: NODE_ENV
        value: production
      - key: APP_URL
        sync: false
      - key: DATABASE_URL
        fromDatabase:
          name: ruleradar-postgres
          property: connectionString
      - key: SYSTEM_CRON_SECRET
        generateValue: true
      - key: SESSION_SECRET
        generateValue: true
      - key: OPENAI_MODEL
        value: gpt-5
      - key: ALERT_FROM_EMAIL
        value: RuleRadar Sweden <alerts@example.com>

services:
  - type: web
    name: ruleradar-web
    runtime: docker
    plan: starter
    rootDir: ruleradar-sweden
    dockerContext: ruleradar-sweden
    dockerfilePath: ruleradar-sweden/infra/docker/Dockerfile.web
    envVarGroups:
      - ruleradar-shared
    healthCheckPath: /api/health
    preDeployCommand: npm run db:migrate
    initialDeployHook: npm run db:seed
    autoDeploy: off

  - type: worker
    name: ruleradar-worker
    runtime: docker
    plan: starter
    rootDir: ruleradar-sweden
    dockerContext: ruleradar-sweden
    dockerfilePath: ruleradar-sweden/infra/docker/Dockerfile.worker
    envVarGroups:
      - ruleradar-shared
    autoDeploy: off

  - type: cron
    name: ruleradar-scan-cron
    runtime: docker
    plan: starter
    rootDir: ruleradar-sweden
    dockerContext: ruleradar-sweden
    dockerfilePath: ruleradar-sweden/infra/docker/Dockerfile.worker
    schedule: "*/30 * * * *"
    envVarGroups:
      - ruleradar-shared
    dockerCommand: npm run scan:run
    autoDeploy: off
